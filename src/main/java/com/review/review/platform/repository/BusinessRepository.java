package com.review.review.platform.repository;

import com.review.review.platform.entity.buisness;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BusinessRepository extends JpaRepository<buisness, Long> {
}
